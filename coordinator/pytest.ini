[pytest]
pythonpath = .
testpaths = tests
asyncio_mode = auto
markers =
    integration: marks tests as integration tests requiring a running coordinator (deselect with '-m "not integration"')
addopts = -m "not integration"
